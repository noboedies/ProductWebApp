# ProductWebApp
A backend project using Servlet.
to run this project
steps:
use Spring tool suite for eclipse https://spring.io/tools#eclipse
download the repo and 
1) in spring tool suite> file > import the folder
2) change the mySql password in HbUtility class in com.tausif package;
